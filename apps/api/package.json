{
  "name": "@voicetypr/api",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "pnpm build && node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.5",
    "@voicetypr/api-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.7.3"
  }
}
